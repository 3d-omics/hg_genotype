# detect sample swaps
---
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - samtools=1.17  # general wrangling
  - bcftools=1.17  # general wrangling
  - picard=3.0.0  # AddOrReplaceGroups to assign library to RG
  - r-tidyverse=2.0.0  # final plot processing
  - r-getopt=1.20.3  # final plot processing
